
/// <reference types="vite/client" />

// Add any custom type definitions here if needed

// Extended subscription category type including 'temp'
interface ExtendedSubscriptionCategories {
  category: 'profile' | 'feed' | 'chat' | 'relay' | 'temp' | 'other';
}
